meta {
  name: File upload
  type: http
  seq: 1
}

post {
  url: http://localhost:8000/upload
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  file: @file(second_site.jpeg)
}

body:file {
  file: @file(first_site.jpeg) @contentType(image/jpeg)
}

settings {
  encodeUrl: true
}
